import { fetchComponent, checkStatus, fetchPaths } from './fetchComponent';
import { traverse } from './traverse';

export default {
  fetchComponent,
  checkStatus,
  fetchPaths,
  traverse
};
